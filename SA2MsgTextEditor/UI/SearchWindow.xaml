<Window x:Name="WindowSearch" x:Class="SA2MsgTextEditor.UI.SearchWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        xmlns:localui="clr-namespace:SA2MsgTextEditor.UI"
        mc:Ignorable="d"
        Title="{DynamicResource Search.Title}"
        Width="640" Height="480"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Loaded="WindowSearch_Loaded">

    <Window.Resources>
        <Style x:Key="DefaultButton" TargetType="Button">
            <Style.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="2"/>
                </Style>
            </Style.Resources>

            <Setter Property="FontSize" Value="14" />
            <Setter Property="Height" Value="25" />
            <Setter Property="BorderBrush" Value="DarkGray" />
        </Style>
        <Style x:Key="DisableOnEmptySearch" TargetType="Button">
            <Style.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="2"/>
                </Style>
            </Style.Resources>

            <Setter Property="FontSize" Value="14" />
            <Setter Property="Height" Value="25" />
            <Setter Property="BorderBrush" Value="DarkGray" />
            <Setter Property="IsEnabled" Value="True" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding Text, ElementName=SearchText, TargetNullValue=''}" Value="{x:Static sys:String.Empty}">
                    <Setter Property="IsEnabled" Value="False" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="TextBox">
            <Style.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="2" />
                </Style>
            </Style.Resources>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Orientation="Vertical" VerticalAlignment="Top" Margin="10">
            <StackPanel Orientation="Horizontal">
                <TextBlock x:Name="FindWhat" Text="{DynamicResource Search.FindWhat}" VerticalAlignment="Center" />
                <TextBox x:Name="SearchText" FontSize="14" Width="210" Height="25" TextAlignment="Left" VerticalContentAlignment="Center" Margin="10,0,0,0" KeyUp="SearchText_KeyUp" />
            </StackPanel>
            <StackPanel Orientation="Vertical" HorizontalAlignment="Left" Margin="0,20,0,0">
                <CheckBox x:Name="IgnoreCase" Content="{DynamicResource Search.IgnoreCase}" Checked="IgnoreCase_Checked" Unchecked="IgnoreCase_Unchecked" />
                <Button x:Name="ButtonFind" Content="{DynamicResource Button.Find}" Style="{StaticResource DisableOnEmptySearch}" Width="150" Margin="0,50,0,0" Click="ButtonFind_Click" />
                <Button x:Name="ButtonClose" Content="{DynamicResource Button.Close}" Style="{StaticResource DefaultButton}" Width="150" Margin="0,20,0,0" Click="ButtonClose_Click" />
            </StackPanel>
        </StackPanel>

        <GroupBox Grid.Column="1" Header="{DynamicResource Search.Results}" Margin="10">
            <Grid Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal">
                    <TextBlock x:Name="ResultsCount" Text="{DynamicResource Search.ResultsCount}" />
                    <TextBlock x:Name="ResultsCountNumber" Text="0" Margin="5,0,0,10"/>
                </StackPanel>
                <ListBox x:Name="SearchResults" Grid.Row="1" BorderBrush="LightGray" SelectionChanged="SearchResults_SelectionChanged">
                    <ListBox.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="3"/>
                        </Style>
                    </ListBox.Resources>
                </ListBox>
            </Grid>
        </GroupBox>
    </Grid>
</Window>
